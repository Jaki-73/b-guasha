# DISABLED — v1 leftover.
#
# v1 was a static site deployable to GitHub Pages. v2 (current) runs a Node.js
# backend (server.js) for accounts, bookings, wallet/QPay and photos, which
# GitHub Pages cannot host. Deploy to a Node host instead — see
# docs/HOSTING-GUIDE.md and docs/PUBLISHING-AND-COSTS.md.
#
# This workflow is kept only so the repository history stays intact. It only
# runs if triggered manually, and then does nothing.

name: (disabled) v1 pages deploy

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v2 needs a Node.js host. See docs/HOSTING-GUIDE.md"
